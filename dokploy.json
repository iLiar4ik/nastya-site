{
  "build": {
    "command": "npm install && npm run db:generate && npx prisma db push --accept-data-loss && npm run build",
    "directory": "."
  },
  "deploy": {
    "command": "npm start",
    "directory": "."
  },
  "env": {
    "NODE_ENV": "production"
  },
  "healthcheck": {
    "path": "/",
    "interval": 30
  },
  "port": 3000
}
