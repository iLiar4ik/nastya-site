# Скопируйте в .env и подставьте свои домены.
# В Dokploy эти переменные задаются в Environment приложения.

# WebSocket сервера комнат (браузер подключается сюда). Обязательно wss:// и ваш домен для room.
EXCALIDRAW_ROOM_WS_URL=wss://excalidraw-room.math-nastya.ru

# Базовый URL API хранилища (сцены/сохранения). Домен, который вы даёте сервису excalidraw-storage-backend.
EXCALIDRAW_STORAGE_URL=https://excalidraw-api.math-nastya.ru
