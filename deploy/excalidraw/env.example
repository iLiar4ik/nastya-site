# Environment для приложения Excalidraw в Dokploy.
# Скопируйте в .env рядом с docker-compose или задайте в Dokploy → Environment.

# WebSocket сервера комнат (wss://, домен = excalidraw-room в Domains).
EXCALIDRAW_ROOM_WS_URL=wss://excalidraw-room.math-nastya.ru

# Базовый URL API хранилища (домен = excalidraw-api в Domains, сервис excalidraw-storage-backend).
EXCALIDRAW_STORAGE_URL=https://excalidraw-api.math-nastya.ru
