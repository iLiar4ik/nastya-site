diff --git a/node_modules/@payloadcms/db-postgres/dist/connect.js b/node_modules/@payloadcms/db-postgres/dist/connect.js
index 846b5d9..45f9e22 100644
--- a/node_modules/@payloadcms/db-postgres/dist/connect.js
+++ b/node_modules/@payloadcms/db-postgres/dist/connect.js
@@ -112,10 +112,8 @@ export const connect = async function connect(options = {
     if (typeof this.resolveInitializing === 'function') {
         this.resolveInitializing();
     }
-    if (process.env.NODE_ENV === 'production' && this.prodMigrations) {
-        await this.migrate({
-            migrations: this.prodMigrations
-        });
+    if (process.env.NODE_ENV === 'production') {
+        await this.migrate(this.prodMigrations?.length ? { migrations: this.prodMigrations } : undefined);
     }
 };
 
