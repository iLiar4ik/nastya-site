# Payload CMS + PostgreSQL
# Для Dokploy задайте эти переменные в Environment Variables панели:
PAYLOAD_SECRET=секретный-ключ-минимум-32-символа
# PostgreSQL (host = имя сервиса PostgreSQL в Dokploy):
DATABASE_URI=postgresql://postgres:ПАРОЛЬ@postgres:5432/postgres
PAYLOAD_UPLOAD_DIR=/app/data/media
NEXT_PUBLIC_SERVER_URL=https://ваш-домен.ru

# После первого деплоя выполните миграции (если админка не открывается):
# docker exec -it <контейнер> npx payload migrate
