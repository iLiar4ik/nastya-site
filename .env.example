# Payload CMS + SQLite (один проект, без PostgreSQL)
# Для Dokploy/Beget задайте PAYLOAD_SECRET и NEXT_PUBLIC_SERVER_URL
PAYLOAD_SECRET=секретный-ключ-минимум-32-символа
# SQLite по умолчанию: file:./data/payload.db (локально) или file:/app/data/payload.db (Docker)
# DATABASE_URL=file:./data/payload.db
PAYLOAD_UPLOAD_DIR=./media
NEXT_PUBLIC_SERVER_URL=https://ваш-домен.ru
