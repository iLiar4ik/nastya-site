# Drizzle + SQLite. В Docker/production задайте file:/app/data/payload.db чтобы приложение и init-db использовали одну БД
DATABASE_URL=file:./data/payload.db
# Загрузки (Docker: /app/data/uploads)
# UPLOAD_DIR=/app/data/uploads

# Публичный URL сайта (HTTPS). Нужен для редиректов (например «Выйти»).
NEXT_PUBLIC_SERVER_URL=https://ваш-домен.ru

# Next.js 15: фиксированный ключ для Server Actions. Обязательно задать на время СБОРКИ (Build),
# иначе после деплоя возможна ошибка "Failed to find Server Action". Сгенерировать: openssl rand -base64 32
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=

# --- Комната урока: LiveKit (видеозвонок, приложение nastya-site в Dokploy) ---
# LIVEKIT_URL=wss://livekit.ваш-домен.ru
# LIVEKIT_API_KEY=your-api-key
# LIVEKIT_API_SECRET=your-api-secret
# Если в консоли «v1 RTC path not found» — обновите сервер LiveKit до актуальной версии.

# --- Доска tldraw (опционально: ключ лицензии для продакшена, иначе предупреждение в консоли) ---
# NEXT_PUBLIC_TLDRAW_LICENSE_KEY=

# После первого деплоя создайте админа:
# npm run db:create-admin email@example.com yourpassword
