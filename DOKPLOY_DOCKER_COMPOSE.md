# Настройка Dokploy с Docker Compose

Это подробная инструкция по настройке проекта в Dokploy с использованием Docker Compose.

## Предварительные требования

- ✅ Dokploy установлен и работает
- ✅ PostgreSQL база данных (будет создана автоматически через docker-compose)
- ✅ Git репозиторий настроен

## Шаг 1: Создание проекта в Dokploy

1. Войдите в Dokploy
2. Нажмите **"New Project"** или **"Создать проект"**
3. Выберите тип: **"Docker Compose"** или **"Compose"**

## Шаг 2: Подключение Git репозитория

1. **Repository URL:**
   ```
   https://github.com/iLiar4ik/nastya-site.git
   ```

2. **Branch:** `master` или `main`

3. **Если приватный репозиторий:**
   - Настройте SSH ключ в Dokploy
   - Или используйте Personal Access Token

## Шаг 3: Настройка Docker Compose

В Dokploy найдите раздел **"Docker Compose Settings"**:

1. **Compose File:** `docker-compose.yml` (должен определиться автоматически)

2. **Project Name:** (опционально) можно оставить пустым, будет использовано имя проекта

## Шаг 4: Переменные окружения

В разделе **"Environment Variables"** добавьте следующие переменные:

### Обязательные переменные:

```
POSTGRES_PASSWORD=ваш_надежный_пароль_для_бд
NEXTAUTH_SECRET=ваш_сгенерированный_ключ
NEXTAUTH_URL=https://ваш-домен.com
```

### Опциональные переменные (можно не указывать, будут использованы значения по умолчанию):

```
APP_PORT=3000
POSTGRES_PORT=5432
POSTGRES_USER=nastya_user
POSTGRES_DB=nastya_site
NODE_ENV=production
COMPOSE_PROJECT_NAME=nastya
```

### Если порт 3000 занят:

Если порт 3000 уже используется, измените `APP_PORT`:

```
APP_PORT=3001
```

И обновите `NEXTAUTH_URL` соответственно:
```
NEXTAUTH_URL=https://ваш-домен.com:3001
```

## Шаг 5: Генерация NEXTAUTH_SECRET

Сгенерируйте секретный ключ:

```bash
# На сервере или локально
openssl rand -base64 32
```

Скопируйте результат и вставьте в переменную `NEXTAUTH_SECRET`.

## Шаг 6: Первый деплой

1. Сохраните все настройки
2. Нажмите **"Deploy"** или **"Deploy Now"**
3. Следите за логами

Процесс деплоя:
- Клонирует репозиторий
- Соберет Docker образ для приложения
- Запустит PostgreSQL контейнер
- Применит схему базы данных
- Запустит приложение

## Шаг 7: Создание первого пользователя

После успешного деплоя создайте первого учителя:

### Способ 1: Через терминал Dokploy

1. В Dokploy откройте проект
2. Найдите раздел **"Terminal"** или **"Console"**
3. Выполните:
   ```bash
   docker-compose exec app node create-teacher.js
   ```

### Способ 2: Через SSH на сервере

1. Подключитесь к серверу по SSH
2. Перейдите в директорию проекта Dokploy:
   ```bash
   cd /etc/dokploy/compose/ваш-проект/code
   ```
3. Выполните:
   ```bash
   docker-compose exec app node create-teacher.js
   ```

### Способ 3: Через docker exec

```bash
docker exec -it nastya-app node create-teacher.js
```

## Шаг 8: Проверка работоспособности

1. Откройте сайт в браузере (URL из `NEXTAUTH_URL`)
2. Проверьте:
   - ✅ Главная страница открывается
   - ✅ Можно войти как учитель
   - ✅ Панель учителя работает

## Управление через Docker Compose

### Полезные команды (через SSH на сервере):

```bash
# Перейти в директорию проекта
cd /etc/dokploy/compose/ваш-проект/code

# Посмотреть статус контейнеров
docker-compose ps

# Посмотреть логи приложения
docker-compose logs -f app

# Посмотреть логи базы данных
docker-compose logs -f postgres

# Перезапустить приложение
docker-compose restart app

# Остановить все
docker-compose stop

# Запустить все
docker-compose start

# Пересобрать и перезапустить
docker-compose up -d --build

# Остановить и удалить контейнеры (данные БД сохранятся)
docker-compose down

# Остановить и удалить ВСЕ включая данные БД (⚠️ осторожно!)
docker-compose down -v
```

## Решение проблем

### Проблема: Порт уже занят

**Решение:**
1. Измените `APP_PORT` в переменных окружения на другой порт (например, 3001)
2. Обновите `NEXTAUTH_URL` соответственно
3. Перезапустите проект

Или освободите порт:
```bash
# Найдите процесс
sudo lsof -i :3000
# Остановите его
sudo kill -9 <PID>
```

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверьте, что контейнер PostgreSQL запущен:
   ```bash
   docker-compose ps
   ```
2. Проверьте логи PostgreSQL:
   ```bash
   docker-compose logs postgres
   ```
3. Убедитесь, что `POSTGRES_PASSWORD` правильный

### Проблема: Ошибка при сборке

**Решение:**
1. Проверьте логи сборки в Dokploy
2. Убедитесь, что Dockerfile правильный
3. Попробуйте пересобрать:
   ```bash
   docker-compose build --no-cache app
   ```

### Проблема: Prisma ошибки

**Решение:**
1. Убедитесь, что команда в docker-compose включает `npx prisma generate` и `npx prisma db push`
2. Проверьте `DATABASE_URL` в переменных окружения
3. Попробуйте выполнить вручную:
   ```bash
   docker-compose exec app npx prisma generate
   docker-compose exec app npx prisma db push
   ```

## Обновление проекта

Когда нужно обновить код:

1. **Локально:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin master
   ```

2. **В Dokploy:**
   - Dokploy автоматически обнаружит изменения
   - Или нажмите **"Redeploy"** вручную

3. **Или через SSH:**
   ```bash
   cd /etc/dokploy/compose/ваш-проект/code
   git pull
   docker-compose up -d --build
   ```

## Резервное копирование базы данных

### Создание бэкапа:

```bash
# Через docker-compose
docker-compose exec postgres pg_dump -U nastya_user nastya_site > backup_$(date +%Y%m%d).sql

# Или напрямую через docker
docker exec nastya-postgres pg_dump -U nastya_user nastya_site > backup.sql
```

### Восстановление:

```bash
# Через docker-compose
docker-compose exec -T postgres psql -U nastya_user nastya_site < backup.sql

# Или напрямую через docker
docker exec -i nastya-postgres psql -U nastya_user nastya_site < backup.sql
```

## Автоматическое резервное копирование

Создайте cron задачу:

```bash
crontab -e
```

Добавьте строку (бэкап каждый день в 2:00):
```bash
0 2 * * * cd /etc/dokploy/compose/ваш-проект/code && docker-compose exec -T postgres pg_dump -U nastya_user nastya_site > /backup/nastya_site_$(date +\%Y\%m\%d).sql
```

## Структура контейнеров

После деплоя будут созданы:

- **nastya-postgres** - контейнер PostgreSQL
- **nastya-app** - контейнер Next.js приложения
- **nastya_postgres_data** - volume с данными БД
- **nastya_network** - сеть для связи контейнеров

## Чек-лист готовности

- [ ] Проект создан в Dokploy
- [ ] Git репозиторий подключен
- [ ] Тип проекта: Docker Compose
- [ ] Все переменные окружения добавлены
- [ ] NEXTAUTH_SECRET сгенерирован
- [ ] Первый деплой прошел успешно
- [ ] Контейнеры запущены (проверено через `docker-compose ps`)
- [ ] Первый пользователь (учитель) создан
- [ ] Сайт открывается в браузере
- [ ] Вход работает

---

**Готово!** Ваш проект должен работать через Docker Compose в Dokploy. 🎉

