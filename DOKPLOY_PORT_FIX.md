# Решение проблемы с портом 3000 в Dokploy

## Проблема

Ошибка при деплое:
```
Error: Bind for 0.0.0.0:3000 failed: port is already allocated
```

Порт 3000 уже занят другим контейнером или процессом.

## Решение 1: Использовать другой порт (рекомендуется)

### В Dokploy добавьте переменную окружения:

```
APP_PORT=3001
```

И обновите `NEXTAUTH_URL`:
```
NEXTAUTH_URL=http://ваш-домен.com:3001
```
или
```
NEXTAUTH_URL=https://ваш-домен.com:3001
```

### После этого:

1. Сохраните настройки
2. Нажмите "Redeploy"
3. Деплой должен пройти успешно

## Решение 2: Освободить порт 3000

Если вы хотите использовать именно порт 3000:

### Шаг 1: Найдите, что использует порт

```bash
# Подключитесь к серверу по SSH
ssh user@ваш-сервер

# Найдите процесс
sudo lsof -i :3000
# или
sudo netstat -tulpn | grep 3000
```

### Шаг 2: Остановите процесс

```bash
# Если это Docker контейнер
docker ps | grep 3000
docker stop <container-id>

# Или если это другой процесс
sudo kill -9 <PID>
```

### Шаг 3: Перезапустите деплой в Dokploy

## Решение 3: Убрать проброс порта (если Dokploy сам управляет)

Если Dokploy сам управляет портами через reverse proxy:

В переменных окружения **НЕ указывайте** `APP_PORT`, или установите его в пустую строку.

Dokploy сам пробросит порт через свой reverse proxy.

## Проверка после исправления

После применения одного из решений:

1. Проверьте статус контейнеров:
   ```bash
   docker-compose ps
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs -f app
   ```

3. Откройте сайт в браузере по указанному URL

## Важно: Исправьте URL репозитория

Также заметил, что Dokploy клонирует `nastya-tutor` вместо `nastya-site`.

**В настройках проекта Dokploy измените Repository URL на:**
```
https://github.com/iLiar4ik/nastya-site.git
```

## Быстрое решение (рекомендуется)

1. В Dokploy добавьте переменную:
   ```
   APP_PORT=3001
   ```

2. Обновите `NEXTAUTH_URL`:
   ```
   NEXTAUTH_URL=http://ваш-ip:3001
   ```
   или если есть домен:
   ```
   NEXTAUTH_URL=https://ваш-домен.com:3001
   ```

3. Нажмите "Redeploy"

Готово! 🎉

